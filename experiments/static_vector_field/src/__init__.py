"""Static post-training vector-field audit package.

Pure static weight-space analysis of a Base->SFT->DPO->RLVR lineage. No data,
no training, no evaluation, no loss, no gradients — only checkpoint weights.
"""
